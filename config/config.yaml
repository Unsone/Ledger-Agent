# Personal Agent 主配置文件

llm:
  provider: "deepseek"
  model: "deepseek-chat"
  temperature: 0.3

agent:
  max_steps: 10
  max_history_turns: 20  # 普通对话保留的最近轮次（system prompt 与 memory 始终保留）
  name: "PersonalAgent"
